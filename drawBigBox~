function bigBox = drawBigbox(x1,y1,x2,y2, filename, fig)
	
	image = importdata(fileName,'jpg');
	
	hold on
	if fig > 0
		figure(fig);
		imshow(difImage);

		line(x1, y1);
		line(x2, y2);
				
		x = [x1 x2 x2 x1 x1];
		y = [y1 y1 y2 y2 y1];
		plot(x, y, 'LineWidth', 2);
	end
	hold off
end
